// object by for in loop

const myObject = {
    'js': 'javascript',
    'py': 'python',
    'java': 'java',
    'cpp': 'c++',
    'cs': 'c#',
    'rb': 'ruby',
    'swift': 'swift'
};

for (const key in myObject) {
    //console.log(`key is ${key} and value is ${myObject[key]}`);
}

